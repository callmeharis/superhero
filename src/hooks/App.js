import React from "react";
import { UseStateComponent } from "./useState/useStateComponent";

function App() {
  return (
    <div>
      <UseStateComponent />
    </div>
  );
}

export default App;

// lifecycle of components
// mounting of component - birth of a component
// update of component - re-rendering of a component
// unmounting of component - death of a component
